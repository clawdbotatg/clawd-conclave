# dependencies
node_modules

# yarn
.yarn/*
!.yarn/patches
!.yarn/plugins
!.yarn/releases
!.yarn/sdks
!.yarn/versions

# eslint
.eslintcache

# misc
.DS_Store

# IDE
.vscode
.idea

# cli
dist

# docker volumes (postgres data)
data/

# local env — glob catches .env, .env.local, .env.development,
# .env.stream, and anything else that starts with ".env."
.env
.env.*
packages/relay/.env
packages/relay/.env.*
